# Deployment wird direkt von Vercel via GitHub-Integration gehandelt.
# Dieser Workflow ist deaktiviert — kein separater CI/CD nötig.
name: Disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deployt direkt via GitHub-Integration."
